# NLP Homework Tasks

Репозиторий с тремя домашними работами по NLP на русском языке.

## Структура проекта

- `первая домашняя работа/первая мини-задача/homework_task1.py`
- `первая домашняя работа/вторая мини-задача/homework_task2.py`
- `вторая домашняя работа/МПИ_25_1_2_Исмаилова_Наргиз_Domain_Shift.ipynb`
- `вторая домашняя работа/README`
- `третья домашняя работа/МПИ_25_1_2_Исмаилова_Наргиз_word2vec_retrieval.ipynb`
- `третья домашняя работа/Анар . Шестой этаж пятиэтажного дома - royallib.ru.fb2`

## Домашняя работа №1

Домашняя работа №1 состоит из двух мини-задач.

### Мини-задача 1: Zipf, предобработка и интенты

Файл: `первая домашняя работа/первая мини-задача/homework_task1.py`

Что делает скрипт:

1. Загружает фрагмент «Белых ночей» (5 страниц) и строит анализ по закону Ципфа.
2. Считает длинный хвост словаря (hapax и слова с частотой `<= 3`).
3. Сравнивает варианты предобработки:
   - raw,
   - lowercase,
   - замена `ё -> е`,
   - удаление коротких слов,
   - лемматизация (`pymorphy3`, если установлен).
4. Определяет интенты через `spaCy Matcher`:
   - базовые (`greeting`, `goodbye`, `ask_weather`, `ask_time`),
   - добавленные (`ask_recommendation`, `express_gratitude`).

Результат выполнения:

- таблицы частот и сравнения словарей,
- графики (Zipf + размер словаря по шагам предобработки),
- распознанные интенты для тестовых фраз.

### Мини-задача 2: токены/стеммы/леммы + NER/POS/DEP

Файл: `первая домашняя работа/вторая мини-задача/homework_task2.py`

Что делает скрипт:

1. Загружает русский художественный текст (фрагмент «Белых ночей»).
2. Сравнивает частоты и словари для:
   - токенов,
   - стемм (`nltk` SnowballStemmer),
   - лемм (`pymorphy3`).
3. Считает долю длинного хвоста (`f <= 3`) для каждого представления.
4. Для 5 предложений показывает:
   - POS/DEP/NER из `spaCy`,
   - сравнение лемм `spaCy` и `pymorphy3`.
5. Извлекает персонажей (NER `PER`) и считает частоты упоминаний.
6. Ищет предложения с приветствиями двумя способами:
   - через `regex`,
   - через `spaCy Matcher`.

Результат выполнения:

- сравнительные таблицы,
- графики по словарям и хвосту,
- список персонажей и найденные приветствия.

## Домашняя работа №2

Основной файл: `вторая домашняя работа/МПИ_25_1_2_Исмаилова_Наргиз_Domain_Shift.ipynb`

Подробное текстовое описание: `вторая домашняя работа/README`

Работа состоит из двух частей.

### Часть A: Domain Shift и распределения

1. Используется датасет `data-silence/rus_news_classifier`.
2. Сравниваются два домена: `politics` и `sports`.
3. Считаются log-odds токены (в обе стороны).
4. Анализируются распределения:
   - длины документов,
   - Zipf-кривые.
5. Проверяется влияние предобработки (стоп-слова, лемматизация).

### Часть B: Authorship Attribution (Naive Bayes)

1. Поэтический корпус делится на чанки.
2. Обучается `MultinomialNB` для определения автора.
3. Оцениваются `accuracy`, `macro F1`, `confusion matrix`.
4. Анализируются важные токены по разнице лог-вероятностей.
5. Сравниваются варианты с лемматизацией и n-граммами.
6. Отдельно проверяется domain shift (длинные тексты -> короткие).

## Домашняя работа №3

Основной файл: `третья домашняя работа/МПИ_25_1_2_Исмаилова_Наргиз_word2vec_retrieval.ipynb`

Датасет: книга Анара «Шестой этаж пятиэтажного дома» в формате FB2.

Работа состоит из трёх частей.

### Часть 1: Готовые русские эмбеддинги (word2vec-ruscorpora-300)

1. Загружается предобученная модель `word2vec-ruscorpora-300` из `gensim-data` (184 973 слова, 300-мерные вектора).
2. Анализируется формат ключей `лемма_POS` (необходимость лемматизации + POS-тега).
3. Для 15 слов из разных доменов (повседневная жизнь, природа, абстрактные понятия, эмоции, действия) выводятся ближайшие соседи.
4. Наблюдения: семантическая близость работает корректно, POS-тег фильтрует часть речи, абстрактные понятия имеют более «размытых» соседей.

### Часть 2: Обучение Word2Vec на своём датасете

1. Парсится книга Анара из FB2 (~1300 абзацев, ~323 тыс. символов).
2. Токенизация: lowercase + кириллические слова длиной > 1.
3. Обучается Skip-gram модель (`vector_size=100`, `window=5`, `min_count=3`, `epochs=50`).
4. Выводятся ближайшие соседи для 10 слов (включая имена персонажей).
5. Сравнение с готовой моделью: своя модель отражает тематическую специфику книги, имена персонажей формируют собственные кластеры.

### Часть 3: Построение retrieval-индекса

1. Текст разбивается на фрагменты по ~200 слов с перекрытием 50 слов (358 фрагментов).
2. Строятся эмбеддинги документов двумя способами:
   - Mean Word2Vec (среднее векторов слов),
   - TF-IDF Weighted Word2Vec (взвешенное среднее).
3. Создаётся FAISS-индекс (`IndexFlatIP` на нормированных векторах = cosine similarity).
4. Тестируется поиск по 5 запросам: «море побережье путешествие», «мать семья дом», «город улица ночь», «любовь чувства женщина», «воспоминания прошлое детство».
5. Выводы: поиск работает осмысленно, TF-IDF взвешивание улучшает релевантность, гранулярность фрагментов достаточна для контекстного поиска.

## Установка зависимостей

```bash
pip install requests numpy pandas matplotlib scikit-learn datasets spacy nltk pymorphy3 pymorphy3-dicts-ru gensim faiss-cpu
python -m spacy download ru_core_news_sm
```

## Запуск

Из корня проекта:

```bash
python "первая домашняя работа/первая мини-задача/homework_task1.py"
python "первая домашняя работа/вторая мини-задача/homework_task2.py"
```

Для домашней работы №2 откройте ноутбук:

```bash
jupyter notebook "вторая домашняя работа/МПИ_25_1_2_Исмаилова_Наргиз_Domain_Shift.ipynb"
```

Для домашней работы №3 откройте ноутбук:

```bash
jupyter notebook "третья домашняя работа/МПИ_25_1_2_Исмаилова_Наргиз_word2vec_retrieval.ipynb"
```

## Примечания

- Для загрузки текстов и датасетов нужен интернет.
- Если не установлен `pymorphy3`, блоки с лемматизацией будут упрощены или пропущены.
- Папка `.venv` содержит локальное окружение и не относится к содержанию домашних работ.
- Для третьей домашней работы файл FB2 уже включён в репозиторий.
